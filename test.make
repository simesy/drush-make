; Make file to test drush make on this system.

options[contrib-destination] = sites/all/contrib

; Contrib modules
core = 6.x
projects[] = drupal

; Contrib modules
projects[] = admin
projects[] = devel
